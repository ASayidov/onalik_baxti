name: onalik_baxti
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Flutter'нинг локализация пакетини қўшиш
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^4.0.4
  google_sign_in: ^5.4.2
  intl: ^0.19.0
  firebase_core: ^3.9.0
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.2.3
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
